apiVersion: v1
kind: Secret
metadata:
  name: titan-secrets
  namespace: titan
  labels:
    app.kubernetes.io/name: titan
type: Opaque
stringData:
  # LLM API Keys (base64 encoded in production)
  # Replace with actual keys or use external secrets manager
  ANTHROPIC_API_KEY: ""
  OPENAI_API_KEY: ""

  # Redis password (optional)
  REDIS_PASSWORD: ""
---
# External Secrets example for production
# Uncomment if using external-secrets-operator
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: titan-external-secrets
#   namespace: titan
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: vault-backend
#     kind: ClusterSecretStore
#   target:
#     name: titan-secrets
#   data:
#     - secretKey: ANTHROPIC_API_KEY
#       remoteRef:
#         key: secret/titan
#         property: anthropic_api_key
